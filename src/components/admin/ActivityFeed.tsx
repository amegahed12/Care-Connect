/**
 * Purpose: The "Recent Activities" list at the bottom.
 * Implementation: It will take a list of activity objects as a prop and map over them to render each row. Each row has an icon, a description, a tag (Doctor, Admin), and a timestamp.
 */